## Как обновить GigaChain?
 
 Выполните команду
 ```bash
 pip install -U gigachain_community
 ```

 ## Как установить gigachain
 
 Выполните команду
 ```bash
 pip install gigachain_community
 ```

 ## Как обновиться с langchain на gigachain
 Нужно создать чистое виртуальное окружение python и активировать его.
 Для Mac/Linux, например так:
 ```bash
 python -m venv venv
 source venv/bin/activate
 ```

 Далее можно установить gigachain:
 ```bash
 pip install gigachain_community
 ```
 
 Ручное удаление langchain не рекомендуется.

 ## Как установить LangGraph
 ```bash
 pip install langgraph
 ```